<% set_meta_tags :description => "Search results for #{@query}.", :canonical => "http://answers.honolulu.gov#{request.fullpath}", :title => "Suchergebnisse für #{@query}." %>

<div class="page-container search">
  <div class="row">
    <div class="span9">
      <div class="main-content">
        <div class="header">
          <h1>Suchergebnisse für: &ldquo;<strong><%=@query%></strong>&rdquo;</h1>

          <% unless (@query_corrected == @query) || (@query_corrected.blank?) %>
            <div class="suggestion">
              Meinst du:"
              <%= link_to @query_corrected, search_path(:q => @query_corrected) %>
              "?
           </div>
          <% end %>

          <br/>
          <% if @results.blank? %>
            <p>
              Entschuldigung, für "<%=@query%>" wurden keine Ergebnisse gefunden. Versuch, die Suche umzuformulieren.
            </p>
          <% else %>
            <ul>
              <% @results.each do |article| %>
                  <li class="article-listing">
                    <h2>
                      <%= link_to article.title, article_path(article.id) %>
                    </h2>
                    <p class="preview"><%= article.preview %></p>
                  </li>
                <% end %>
            </ul>
          <% end -%>

        </div>
      </div>
    </div>
    <div class="span3">
      <%= render :partial => 'shared/categories_sidebar' %>
    </div><!-- /span3 -->
  </div>
</div>
