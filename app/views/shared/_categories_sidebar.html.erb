<div class="sidebar">
  <div class="sidebar-unit">
    <h4 class="side-head">ALLE KATEGORIEN</h4>
    <div class="sidebar-content">
       <ul>
        <% Category.order(:name).each do |c| %>
          <%= content_tag( :li, link_to(c.name, category_path(c.id))) %>
        <% end -%>
       </ul>
    </div>
  </div>
</div>
